# Star Wars Search – Backend

Simple Node.js + Express server that proxies requests to the public SWAPI
and keeps an in-memory search history.

## Requirements

- Node.js 18+
- npm

## Installation

```bash
npm install
npm run dev
